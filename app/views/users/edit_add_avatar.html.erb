<%= form_for(@user, :html => { :multipart => true }, :url => update_add_avatar_user_path(@user)) do |f| %>
  <h3 id="loud">Dodaj Avatar</h3>
  <div class="field">
   <%= f.file_field :avatar %>
  </div>
  <p><%= f.submit "Zapisz" %></p>
<% end %>
